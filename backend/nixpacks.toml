[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["curl -LsSf https://astral.sh/uv/install.sh | sh && /root/.local/bin/uv pip install -r requirements.txt --system"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
